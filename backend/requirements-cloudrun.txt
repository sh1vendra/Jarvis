# Dependencies for the Cloud Run deployment of the agent server ONLY.
#
# This is the subset of backend/requirements.txt that the Gemini-facing
# agent pipeline actually needs at import + run time on Linux. Deliberately
# excludes:
#   - pyobjc-framework-* : macOS Accessibility/Quartz bindings. Don't exist
#     on Linux; tools/mac_control.py and tools/perception.py guard their
#     imports and raise a clear error if the Mac-only functions are called.
#   - sounddevice, openwakeword, onnxruntime : microphone capture and
#     wake-word detection (voice/capture.py, voice/wakeword.py). There is
#     no microphone in this container - voice/wakeword.py's own guarded
#     imports mean agent_server.py still imports cleanly without these
#     installed (confirmed directly by building and running this exact
#     image - see planning.md), it just reports wake word unavailable,
#     and start_wakeword_listener() is never even called on this path
#     (only reached from _main(), which the Cloud Run branch skips).
#
# Pinned to the versions running locally so the container is reproducible.

google-adk==2.6.3
google-genai==2.17.0
python-dotenv==1.2.2
pydantic==2.13.4
dateparser==1.4.2
Pillow==12.3.0
websockets==15.0.1
SpeechRecognition==3.17.0
